%\title{ {{title}} }

%\begin{document}
\includepdf[pages=-]{include/pdf/mfnf_prelude-{{subtarget}}.pdf}
{{#each parts}}
    {{~ #unless (part_excluded this ../subtarget)}}
        \part{ {{~ this.title ~}} }
        {{~ #each this.chapters ~}}
            {{~ #unless (chapter_excluded this.markers.exclude ../../subtarget)}}
                \chapter{ {{~ this.title ~}} }
                \input{\articlespath/{{escape_make this.path}}/{{this.revision ~}}}
                {{#if this.markers.after ~}}
                \includepdf[pages=-]{include/pdf/{{this.markers.after.path}}-{{../../subtarget}}.pdf}
                {{/if ~}}
            {{/unless ~}}
        {{~ /each ~}}
    {{~ /unless ~}}
{{/each}}
\listoffigures
\includepdf[pages=-]{include/pdf/mfnf_epilogue-{{subtarget}}.pdf}
%\end{document}
